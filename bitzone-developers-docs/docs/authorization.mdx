---
id: authorization
title: Авторизация
description: Доступ к порталу документации
---

import React, { useState } from 'react';

export const SESSION_COOKIE = 'DOCS_SESSION';
export const SESSION_VALUE = 'ok';

export function AuthForm() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState(null);

  function onSubmit(e) {
    e.preventDefault();
    if (username === 'admin' && password === 'admin') {
      document.cookie = SESSION_COOKIE + '=' + SESSION_VALUE + '; SameSite=Lax; Path=/; Max-Age=86400';
      window.location.replace('/');
      return;
    }
    setError('Неверный логин или пароль');
  }

  return (
    <form onSubmit={onSubmit} className="auth-card">
      <h2>Авторизация</h2>
      {error ? <p className="auth-error">{error}</p> : null}
      <label htmlFor="username">Логин</label>
      <input id="username" value={username} onChange={(e)=>setUsername(e.target.value)} required />
      <label htmlFor="password">Пароль</label>
      <input id="password" type="password" value={password} onChange={(e)=>setPassword(e.target.value)} required />
      <button type="submit">Войти</button>
      <div className="auth-hint">По умолчанию: admin / admin</div>
    </form>
  );
}

<style>
{`
.auth-card{max-width:420px;padding:16px;border:1px solid var(--ifm-toc-border-color);border-radius:10px}
.auth-card input{width:100%;padding:10px;border-radius:8px;margin:6px 0}
.auth-card button{margin-top:12px;padding:10px 14px;border-radius:8px}
.auth-error{color:#dc2626}
.auth-hint{margin-top:8px;opacity:.8;font-size:12px}
`}
</style>

<AuthForm />
